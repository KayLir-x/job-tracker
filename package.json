{
  "name": "job-tracker",
  "version": "1.0.0",
  "description": "Full-stack job application tracker with React, Node.js, Express, Prisma, SQLite, JWT authentication, document uploads, cover letters, status tracking, and dashboard analytics.",
  "scripts": {
    "build": "npm install --prefix server && npm install --include=dev --prefix client && npm run build --prefix client && npm run prisma:generate --prefix server && npm run prisma:migrate --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": "20.x"
  }
}